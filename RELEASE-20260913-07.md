# 2026-09-13 第七版：iPhone 直接分享牌陣圖片

## 修正

- 原功能只有 PNG 下載與另開圖片；使用 Safari 工具列分享時，仍可能分享網頁網址。
- 圖片預覽新增第一順位的「分享圖片」。PNG 產生完成後先建立 `File`（`image/png`），下一次按鈕點擊直接 `navigator.share({ files: [file] })`，不帶網址、文字或標題，不重新下載／轉檔。
- 以實際 PNG File 檢查 `navigator.canShare`，在 HTTPS 與支援檔案分享的瀏覽器啟用。分享不支援或失敗時保留下載／長按另存，不降級成網址分享。
- 分享中防止重複開啟；取消後可分享同一張圖片。關閉預覽會清除保留的檔案；舊分享結果不會覆蓋新預覽的訊息。
- 維持完整牌陣 PNG、隨機結果、翻牌、3D 牌盒、文字位置與其他既有流程。

## iPhone 驗收

開啟 <https://rayjingreal-ux.github.io/tarot/?v=20260913-07> → 完成抽牌 →「儲存牌陣」→ 圖片準備好後按「分享圖片」→ 在系統分享中選擇目標 App 或儲存影像。

請使用站內的「分享圖片」，不是 Safari 工具列的分享。檢查目標 App 顯示的是 PNG 圖片附件／預覽，且包含主牌、文字與切牌，沒有網站連結。取消後再試一次；既有「下載 PNG 圖片」仍可單獨使用。

使用者實機為 iPhone；開發環境僅能進行瀏覽器尺寸檢查與模擬分享 API 測試，未聲稱已在 iPhone 原生分享視窗驗證或傳送給聯絡人。

實作依據：[WebKit 使用者啟動限制](https://webkit.org/blog/13862/the-user-activation-api/)、[W3C Web Share API](https://www.w3.org/TR/web-share/)。

## 開發驗證

- 全套 118 項自動測試通過，其中 27 項新增分享測試涵蓋 PNG bytes／MIME／檔名一致、僅傳檔案、點擊同步呼叫、取消重試、跨預覽防重複、舊回應隔離、不支援與錯誤備援。
- JavaScript 語法與 diff 檢查通過。
- 本機 390×844 瀏覽器尺寸完成抽牌及產生 PNG，確認分享入口、下載、另存與 iPhone 操作提示，關閉重開預覽可再次準備分享，沒有瀏覽器錯誤；未向任何聯絡人實際傳送測試圖片。

## 回退

改版前標籤 `backup/pre-image-share-20260913` 保留第六版 `b6dc17ef5634d43547b247ee3653611bc20b3cf9`；可 revert 此版提交後正常推送，不需強制改寫歷史。
