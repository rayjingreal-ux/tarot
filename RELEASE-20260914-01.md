# 2026-09-14：選填問題與牌陣回憶

## 新增功能

- 抽牌前的牌陣選單上方新增半透明輸入圖框，以原生 SVG 月相、星芒和符文幾何裝飾，不新增點陣圖片下載或其他彈出視窗。
- 問題選填，最多 200 字元，可換行，留白不阻擋抽牌。手機輸入字級 16px；短螢幕可捲動到牌陣及開始按鈕。
- 開始洗牌前建立當次問題快照，重洗保留同一問題；載入失敗可保留文字重試，完成抽牌或取消後開啟新一輪則清空。
- 結果主牌陣下方顯示「當時的問題」，自動預留空間，不遮擋主牌、左下切牌及操作列；長問句可內部捲動，單張放大也可閱讀。未填不顯示、不留空位。
- 完整 PNG 在主牌與切牌下方加入問題框，保留完整文字、換行；手機畫面未顯示的文字也完整輸出。一般單欄；極端大量換行時分欄以維持最大高 4096px。
- iPhone「分享圖片」沿用同一張 PNG，問題直接存入圖片，不是另外分享文字或網址。預覽中明確提示有填寫的問題也會包含。
- 文字以 `textContent`／Canvas 純文字輸出，不插入 HTML；不寫入 localStorage、不另行傳送問題到伺服器。刷新頁面不保留本輪內容，請下載或分享圖片留存。

## 驗證

- 133 項自動測試通過，包含兩種抽牌方式、重洗、重試、取消與新一輪、空白與特殊字元、中文輸入組字、十四牌陣、手機問句留位及完整 PNG。
- 瀏覽器實際檢查 1280 × 720 桌面、390 × 844 手機及 320 × 568 短螢幕：問題框、牌陣與開始按鈕可操作，短螢幕可捲動；結果問題可獨立捲動閱讀，不遮住切牌及操作列。
- 以含中文、換行與月亮符號的問題完成十二宮位流程，下載並目視檢查 1600 × 3130 PNG：完整主牌、切牌、牌位標題、牌名、正逆位及問題均保留，無操作按鈕。
- 實際確認重新抽牌會清空上一輪文字；留白完成單張流程，結果不顯示問題框。瀏覽器無警告或錯誤。
- iPhone 原生鍵盤與系統分享視窗仍需實機驗收；本次以手機尺寸瀏覽器及既有檔案分享測試驗證。

## 驗收與回退

入口：<https://rayjingreal-ux.github.io/tarot/?v=20260914-01>

選牌 →「抽牌」→ 選填問題並選牌陣 → 洗牌／切牌／抽牌 → 查看牌陣下方問題 →「儲存牌陣」→「下載 PNG 圖片」或「分享圖片」。另以留白、新一輪清空及手機十二宮位檢查。

改版前標籤 `backup/pre-optional-question-20260914` 保留前版 `876feef6714ebad076daf3d0bc5bc7ec16cad2c0`。需要回退時 revert 本版提交後正常推送，不必強制改寫歷史。
