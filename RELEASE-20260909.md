# 2026-09-09 單張牌儀式驗收版

## 保留的架構

保留四副牌組的 3D 牌盒輪播、中央旋轉光輝、牌盒模型與素材、分段開盒、原有抽牌舞台與收盒動畫。一般瀏覽與三個校正工作檯維持獨立。

本版借鏡參考站的抽牌節奏，新增流程控制及介面，未複製其圖像、文案或程式碼。多張牌陣、AI 解讀、歷史紀錄不在這次版本範圍。

## 驗收路徑

1. 從藏書室走近書櫃，左右滑動，確認四副牌盒皆可選、只有中央牌盒持續旋轉。
2. 選一副牌。書型牌盒依原流程開盒、取說明書、取內盒／牌堆；新增文字按鈕可完成相同步驟。The Unveiled 可從抽牌分頁進入。
3. 留下一個問題（可空白、最多 200 字），選擇手動洗牌或星光選牌。問題只保留在本次頁面的記憶體，不上傳、不永久儲存。
4. 手動：按住洗牌約 1.8 秒，放開暫停；鍵盤也可按住空白鍵，或按 Enter 自動完成洗牌。完成後按「洗好了，展牌」，左右滑動牌背選一張。也可交給自動選牌。
5. 星光：等待原舞台動畫完成，自動選定一張。
6. 選定牌以牌背進入原 3D 舞台，點中央牌或「翻至正面」揭曉。揭曉前不顯示牌名與編號；揭曉後可旋轉觀察與翻回牌背。
7. 本輪結果鎖定，不能用上下張、方向鍵或目錄換結果。「重新抽牌」開始新一輪；取消時保留上一輪結果。「收回牌盒」保留原歸位流程。
8. 在設定、洗牌、選牌各階段試按返回或 Escape，再重新進入，應能繼續使用。一般瀏覽仍可看全部牌面並打開校正工作檯。

桌面可用滑鼠拖曳、滾輪、左右箭頭選牌；鍵盤可用方向鍵、Home、End 移動牌背焦點，Enter 選取。手機使用原生水平滑動。系統的減少動態效果設定會縮短／停止新增裝飾動畫。

## 抽牌規則與失敗恢復

- 以 Fisher–Yates 洗牌，每輪只允許選定一次；洗牌順序與所選牌在同一輪保持穩定。
- Woodland、Red Visions 各 78 個概念，The Unveiled 80 個獨立概念。
- Prose Poem 的 80 張圖稿對應 78 個概念；教皇、寶劍十各自的 A/B 先等機率選一個圖稿，再一起洗牌，因此不提高這兩個牌義的中選率。
- 牌組清單失敗可重試；選中牌面失敗時保留同一張，重試只重新載入，不改抽牌結果，也不以別副牌面代替。
- 重新整理頁面會清除本輪結果與問題；本版沒有帳號、伺服器抽牌或持久化紀錄。

## 備份與回退

新版開發前的穩定版本已建立 annotated tag，並推送 GitHub：

- 標籤：`backup/pre-draw-ritual-20260909`
- 提交：`519f6ab114dc6f5807ecf0553d17367e2e1fa3dd`
- 遠端：<https://github.com/rayjingreal-ux/tarot/tree/backup/pre-draw-ritual-20260909>

若需回退正式網站，對新版提交做 `git revert <新版提交 SHA>`，測試後推送 `main`，即可保留完整提交歷史。不要使用強制推送或 `reset --hard`。若版本發布後還有其他改動，先檢查其相依性與衝突，勿直接覆蓋。

僅想另外檢視備份，可在乾淨工作目錄建立獨立工作樹，不影響目前版本：

```powershell
git worktree add ../tarot-before-draw-ritual backup/pre-draw-ritual-20260909
```

## 自動驗證

```powershell
node --check app.js
node --check draw-ritual.js
node --test tests/draw-session.test.mjs
```

測試涵蓋四份實際牌組清單、變體分組、穩定 ID、可注入亂數、單次鎖定與非法操作拒絕。3D 視覺與實際操作需依上述路徑驗收。

本機發布前檢查：10 項抽牌測試通過；316 張牌面、316 張縮圖及四副牌背共 636 個引用全數存在並可 HTTP 載入；入口、模組、CSS 與 manifest 的 14 個檢查全數通過。新增介面已以 390 × 667 與 820 × 500 視窗實測。
