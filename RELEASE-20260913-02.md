# 2026-09-13 第二版：圖案式牌陣、光手重洗與載入優化

保留既有四副牌組、牌盒 3D 結構、中央旋轉展示、牌圖校正／透視與工作檯。仍使用現有靜態 GitHub Pages 專案，沒有替換框架或託管服務。

## 新流程

心裡默念問題 → 底部滑出十四種牌陣圖案按鈕 → 親手洗牌／迎接命運 → 旋轉、四散、收攏、炸散展開 → 可點中央光手重洗 → 開始切牌 → 抽主牌 → 翻牌。

- 移除問題欄位與牌陣下拉選單。橫向牌陣列可觸控滑動、滑鼠滾輪、Tab 移動並直接點選；每次返回再開啟都可重新選擇。
- 中央光手取代底部洗牌選單。第一輪可長按；放開暫停進度，空白鍵可長按，Enter 可自動跑完。完成後再次點手或按 Enter／空白鍵會重洗完整一輪。
- 完成洗牌不會自動切牌。「開始切牌」只在完成後出現，重洗時隱藏。釋放原本長按不會誤觸下一輪。
- 重洗實際建立新的隨機牌序，保留牌陣選擇；切牌和主牌尚未選定。切牌後不再允許重洗，以免改動已選結果。
- 「迎接命運」取代原星光選牌名稱。自動洗牌完成後也能重洗，仍先切牌再自動補齊主牌。
- 維持切牌不計入張數、總覽左下角、主牌互不重複、獨立隨機正逆位與失敗重試不重抽。
- 低動態偏好保留靜態牌背與柔光，不執行旋轉四散動畫。切換分頁會暫停自動洗牌計時。

## 載入改動與量測範圍

| 材質 | 舊版 | 新版 |
| --- | ---: | ---: |
| 初次進入選牌前等待 | 61 張／11,109,261 bytes | 26 張外盒面／3,843,313 bytes |
| 四副全部用到的材質 | 61 張／11,109,261 bytes | 55 張／9,036,273 bytes |
| 八張 PNG 的優先編碼 | 4,491,519 bytes | 無損 WebP 3,138,948 bytes |

阻擋初次選牌的材質位元組減少 65.4%。內盒、說明書和牌背依選中的副牌載入，合併重複請求、快取成功結果，失敗可重試；新編碼失敗會回退原檔。六張未使用的非直立內盒面不再預載，但沒有刪檔。載入中可 Escape 取消或改選另一副牌；初始化未完成前一般瀏覽不允許提早進場。

八張新 WebP 已逐像素核對解碼 RGBA、尺寸與 ICC，相同於原 PNG。沒有縮小牌面、降低品質或變動透明圓角。原始 PNG 全部保留。

未顯示的鑑賞舞台不再持續更新和繪製；輪播仍正常運作。洗牌階段不再預抓五張無關牌面。這些都是程式和 HTTP／檔案量測，不是瀏覽器 LCP、FCP 或實機幀率；不可將 65.4% 誤解成頁面時間縮短比例。先前 HTTP/1.1 六路下載舊版 61 張材質單次需 14.399 秒，只作當時網路條件紀錄。

## 驗收與測試

1. 選任一牌盒並進入抽牌，確認只有心裡默念的提示，底部滑入圖案按鈕，沒有輸入欄位或下拉選單。
2. 選三張，長按中央光手，觀察四段動作；中途放開可續按。走完停在展開狀態，不自行切牌。
3. 點光手連續重洗兩次，每次走完完整動作；按「開始切牌」後才顯示切牌選擇，切牌不佔三張主牌。
4. 抽完三張收回牌盒，再進抽牌選七張、十二宮位；確認主牌張數依新選擇，切牌仍另外一張。
5. 測試「迎接命運」、鍵盤、切換分頁、Escape 取消，以及換副牌。確認原牌盒旋轉／開盒、一般瀏覽和工作檯仍可使用。
6. 手機直式和桌面檢查光手置中、底部按鈕滑動、牌卡收攏與炸散效果。

自動驗證：`node --test tests/*.test.mjs` 共 42 項；包括十四種牌陣反覆開啟、實際控制器的長按／重洗／切牌狀態、亂序與正逆位、光效幾何、紋理載入去重／重試與 HTML ID 綁定。另有 JS 語法檢查與 `python scripts/optimize_texture_encodings.py` 無損核對。

本輪未執行瀏覽器視覺／實機互動測試，以上 Node 測試不能替代使用者的畫面驗收。Web Performance 技能原用的 DevTools 量測工具在本次環境不可用，因此沒有宣稱 Core Web Vitals 分數。

## 備份與回退

- 改版前提交：`d27198018de4ae301dc6965c506ca5f4c301424f`
- 備份標籤：`backup/pre-performance-20260913`
- [GitHub 備份版本](https://github.com/rayjingreal-ux/tarot/tree/backup/pre-performance-20260913)

回退時，確認工作目錄沒有未保存修改，對本次正式提交執行 `git revert <提交 SHA>`，測試後推送 `main` 即可重新部署。不需 force push 或刪除歷史。其他已建立的備份標籤也保留。
